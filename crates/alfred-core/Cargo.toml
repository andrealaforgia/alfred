[package]
name = "alfred-core"
version = "0.1.0"
edition = "2021"
description = "Pure domain core for the Alfred text editor"

[dependencies]
regex = "1"
ropey = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
